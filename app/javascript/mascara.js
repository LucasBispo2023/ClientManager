/* const inputPhone = document.querySelector("#telephone-field");
const inputZipCode = document.querySelector("#zip_code");

// Phone Editing Mask
inputPhone.addEventListener('keypress', function () {
    let inputlength = inputPhone.value.length;

    if (inputlength === 0) {
        inputPhone.value += '(';
    } else if (inputlength === 3) {
        inputPhone.value += ')';
    } else if (inputlength === 9) {
        inputPhone.value += '-';
    }
});

// Zip Code Editing Mask
inputZipCode.addEventListener('keypress',function(){
    let inputlength2 = inputZipCode.value.length;

    if (inputlength2 === 5){
        inputZipCode.value += '-';
    }
}) */
